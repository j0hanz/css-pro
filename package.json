{
  "name": "css-pro",
  "version": "0.7.0",
  "private": true,
  "description": "Claude Code plugin that stops agents wrecking your CSS.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "format": "prettier --write .",
    "test": "node --test"
  },
  "devDependencies": {
    "prettier": "^3.8.4"
  }
}
