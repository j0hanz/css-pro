{
  "name": "css-pro",
  "version": "0.5.0",
  "private": true,
  "description": "Claude Code plugin that stops agents wrecking your CSS.",
  "scripts": {
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.8.4"
  }
}
